---
import Icon from './Icon.astro';
---

<section class="box skills">
	<div class="stack gap-2 lg:gap-4">
		<Icon icon="code" color="var(--accent-regular)" size="2.5rem" gradient />
		<h2>Backend</h2>
		<p>Development of Restful APIs, Realtime Chats, E-commerce/E-learning/Monitoring websites</p>
	</div>
	<div class="stack gap-2 lg:gap-4">
		<Icon icon="web" color="var(--accent-regular)" size="2.5rem" gradient />
		<h2>Frontend</h2>
		<p>Development of responsive UI in Tailwind, Bootstrap or CSS, usage of common JS packages</p>
	</div>
	<div class="stack gap-2 lg:gap-4">
		<Icon icon="server" color="var(--accent-regular)" size="2.5rem" gradient />
		<h2>SysAdmin</h2>
		<p>Network configuration, Linux-Based distros Administration, setting up tech stacks, basic CI/CD and more</p>
	</div>
</section>

<style>
	.box {
		border: 1px solid var(--gray-800);
		border-radius: 0.75rem;
		padding: 1.5rem;
		background-color: var(--gray-999_40);
		box-shadow: var(--shadow-sm);
	}

	.skills {
		display: flex;
		flex-direction: column;
		gap: 3rem;
	}

	.skills h2 {
		font-size: var(--text-lg);
	}

	.skills p {
		color: var(--gray-400);
		font-size: 15px;
	}

	@media (min-width: 50em) {
		.box {
			border-radius: 1.5rem;
			padding: 2.5rem;
		}

		.skills {
			display: grid;
			grid-template-columns: repeat(3, 1fr);
			gap: 5rem;
		}

		.theme-dark .skills {
			background: #0a0c12;
		}

		.skills h2 {
			font-size: var(--text-2xl);
		}
	}
</style>
